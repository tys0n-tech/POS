# ==============================================================================
# Northline Café POS - Environment Template (.env.example)
# Copy this file to .env and replace with your actual configuration values
# ==============================================================================

# Application Port & Mode
PORT=3000
NODE_ENV=development

# ------------------------------------------------------------------------------
# Supabase Configuration (Frontend Realtime & Cloud Database)
# ------------------------------------------------------------------------------
# Get these values from your Supabase Project Settings -> API
VITE_SUPABASE_URL="https://your-project-ref.supabase.co"
VITE_SUPABASE_ANON_KEY="your-supabase-anon-public-key"

# ------------------------------------------------------------------------------
# Prisma Database Connection URL
# ------------------------------------------------------------------------------
# Option A: Supabase PostgreSQL (Recommended)
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres"

# Option B: Local PostgreSQL Server
# DATABASE_URL="postgresql://postgres:password@localhost:5432/cafe_pos?schema=public"

# Option C: Local SQLite File (Zero config setup)
# DATABASE_URL="file:./dev.db"

# ------------------------------------------------------------------------------
# Hardware Peripherals & Device Bridges
# ------------------------------------------------------------------------------
# 1. Smart EDC Credit Card Terminal (LAN IP / Bluetooth / Web Serial)
# Options: DISABLED | IP_LAN | BLUETOOTH | USB_SERIAL
VITE_EDC_TERMINAL_TYPE="IP_LAN"
VITE_EDC_TERMINAL_IP="192.168.1.150"
VITE_EDC_TERMINAL_PORT=8080
VITE_EDC_MERCHANT_ID="MERCHANT-NORTHLINE-01"

# 2. Digital Weight Scale (Coffee Dose / Retail Scale)
# Options: DISABLED | SIMULATOR | WEB_SERIAL | BLUETOOTH_BLE
VITE_SCALE_TYPE="SIMULATOR"
VITE_SCALE_BAUD_RATE=9600
VITE_SCALE_UNIT="g"

# 3. Network ESC/POS Thermal Receipt Printer (RAW TCP port 9100)
VITE_NETWORK_PRINTER_IP="192.168.1.200"
VITE_NETWORK_PRINTER_PORT=9100

# ------------------------------------------------------------------------------
# Security & Authentication
# ------------------------------------------------------------------------------
# JWT secret key for signing staff sessions
JWT_SECRET="your_jwt_secret_key_change_me_in_production"
